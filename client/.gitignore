# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Not committed: a Windows-generated lockfile omits Linux-only optional deps
# (rollup/emnapi), which breaks `npm ci` on Railway. Without a lockfile the
# build runs `npm install`, which resolves the correct platform deps.
package-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
